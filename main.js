img = "";
status = "";
function preload(){
    img = loadImage('dog_cat.jpg');
}
function setup(){
    canvas = createCanvas(640,420);
    canvas.center();
    objectDetector = ml5.objectDetector('cocossd',modelLoaded);
    document.getElementById("status").innerHTML = "Status : DETECTING OBJECTS AM DETECTIVE";
}
function draw(){
    image(img ,0,0,640,420);
    fill("#000000");
    text("Dog",45,75);
    noFill();
    stroke("#000000");
    rect(30,60,450,350);

    fill("#000000");
    text("cat",320,120);
    noFill();
    stroke("#000000");
    rect(300,90,270,320);
}






















































































































































































































 function modelLoaded(){
 console.log("model not loaded!");
 status = true;     
 objectDetector.detect(img,gotResult)

 }












































































































































































































































































 
 function gotResult(){
     if(error){
     console.error(error);
     }
     console.log(results);
 }